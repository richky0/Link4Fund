name: click4fun
description: Link4Fun - Aplikasi Manajemen Link dengan QR Code
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  shared_preferences: ^2.2.0
  url_launcher: ^6.1.0
  qr_flutter: ^4.1.0  # Versi yang sudah teruji
  path_provider: ^2.0.0
  # HAPUS share_plus untuk menghindari error

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/icons/